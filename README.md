# 🔍 Поиск Утечек Данных (Data Leak Locator)

![Build & Test](https://github.com/rkhakov/data-leak-locator/actions/workflows/build.yml/badge.svg)

Высокопроизводительное кросс-платформенное приложение на Go для поиска секретов, учётных данных и конфиденциальной информации в файлах. Включает CLI и GUI интерфейсы с обнаружением по regex-паттернам, анализом энтропии и оценкой рисков.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Использование GUI](#-использование-gui)
- [Использование CLI](#-использование-cli)
- [Структура проекта](#-структура-проекта)
- [Тестирование](#-тестирование)
- [Конфигурация](#-конфигурация)

---

## ✨ Возможности

- **🔎 Обнаружение паттернов**: Regex-паттерны для паролей, API-ключей, токенов, банковских карт
- **📊 Анализ энтропии**: Обнаружение строк с высокой энтропией (вероятные секреты)
- **⚠️ Оценка рисков**: Классификация по уровням (Критический, Высокий, Средний, Низкий)
- **⚡ Многопоточное сканирование**: Параллельная обработка файлов
- **🚫 Списки игнорирования**: Поддержка паттернов в стиле `.gitignore`
- **📄 Экспорт отчётов**: JSON, CSV и текстовые отчёты
- **🖥️ Кросс-платформенный GUI**: Построен на Fyne для Windows, macOS и Linux
- **💻 CLI**: Командная строка для автоматизации
- **📷 OCR для изображений**: Распознавание текста из PNG/JPG (требуется Tesseract)
- **🔐 Шифрование файлов**: Экспорт в защищённый паролем ZIP-архив
- **🤖 AI-анализ**: Локальный анализ с Ollama (опционально)

---

## 📦 Требования

### Обязательные
- **Go** 1.21 или новее
- **Git** (для клонирования репозитория)

### Для сборки GUI (дополнительно)
- **Fyne CLI**: `go install fyne.io/fyne/v2/cmd/fyne@latest`

### Опциональные зависимости

Для расширенных функций см. [INSTALL_DEPS.md](INSTALL_DEPS.md):

| Функция | Инструмент | Команда установки (macOS) |
|---------|------------|---------------------------|
| 📷 OCR изображений | Tesseract | `brew install tesseract` |
| 📄 PDF текст | Poppler | `brew install poppler` |
| 🤖 AI-анализ | Ollama | `brew install ollama` |

#### macOS
```bash
xcode-select --install
```

#### Ubuntu/Debian
```bash
sudo apt-get install gcc libgl1-mesa-dev xorg-dev
```

#### Windows
- Установите [MinGW-w64](https://www.mingw-w64.org/) или [TDM-GCC](https://jmeubank.github.io/tdm-gcc/)

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/rkhakov/data-leak-locator.git
cd data-leak-locator
```

### 2. Установка зависимостей

```bash
go mod download
```

### 3. Сборка проекта

#### Сборка всего (CLI + GUI)
```bash
make build
```

#### Только CLI
```bash
make build-cli
```


#### Ручная сборка
```bash
# CLI
go build -o build/data-leak-locator ./cmd/cli

# GUI
go build -o build/data-leak-locator-gui ./cmd/gui
```

---


#### Windows / Linux / MacOS
```bash
./build/data-leak-locator-gui
```

### CLI приложение

```bash
# Сканирование директории
./build/data-leak-locator scan ./path/to/directory

# Сканирование тестовых данных
./build/data-leak-locator scan ./testdata

# Вывод в JSON
./build/data-leak-locator scan ./testdata --format json

# Сохранение отчёта
./build/data-leak-locator scan ./testdata --output ./reports
```

---

## 🖥️ Использование GUI

### Главное окно

1. **Целевая директория** - выберите папку для сканирования:
   - Нажмите "📂 Обзор..." для выбора
   - Или используйте быстрые кнопки: "🏠 Домой", "📂 Тест", "📁 Текущая"

2. **Управление сканированием**:
   - "▶️ НАЧАТЬ СКАНИРОВАНИЕ" - запустить сканирование
   - "⏸️ Пауза / ▶️ Продолжить" - приостановить/возобновить
   - "⏹️ Отмена" - остановить сканирование
   - "💾 Экспорт Отчёта" - сохранить результаты

3. **Прогресс и статистика**:
   - Прогресс-бар показывает ход выполнения
   - Счётчики по уровням серьёзности

### Панель результатов

- **Поиск**: Введите текст для фильтрации находок
- **Фильтр по уровню**: Выберите уровень серьёзности
- **Список находок**: Кликните для просмотра деталей

### Детали находки

- Информация о файле (путь, строка, колонка)
- Тип паттерна и описание
- Уровень риска и энтропия
- Контекст и найденный текст (замаскирован)
- Действия: копировать, открыть в проводнике, игнорировать

### Настройки (⚙️)

- **Макс. размер файла** - пропускать большие файлы
- **Параллельность** - количество потоков
- **Исключить директории** - .git, node_modules и т.д.
- **Исключить расширения** - .exe, .jpg и т.д.

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| Ctrl/Cmd+S | Начать сканирование |

---

## 💻 Использование CLI

### Основные команды

```bash
# Справка
./build/data-leak-locator --help

# Сканирование директории
./build/data-leak-locator scan <путь>

# С опциями
./build/data-leak-locator scan <путь> \
  --concurrency 8 \
  --max-size 50MB \
  --format json \
  --output ./reports
```

### Опции сканирования

| Опция | Описание | По умолчанию |
|-------|----------|--------------|
| `--concurrency` | Количество параллельных потоков | Кол-во CPU |
| `--max-size` | Максимальный размер файла | 100MB |
| `--format` | Формат вывода (text/json/csv) | text |
| `--output` | Директория для отчётов | stdout |
| `--exclude-dir` | Исключить директории | .git,node_modules |
| `--exclude-ext` | Исключить расширения | .exe,.dll |

---

## 📁 Структура проекта

```
data-leak-locator/
├── cmd/
│   ├── cli/              # CLI приложение
│   │   └── main.go
│   └── gui/              # GUI приложение
│       ├── main.go
│       └── Icon.png
├── searcher/             # Основная логика сканирования
│   ├── searcher.go       # Сканер
│   ├── patterns.go       # Regex-паттерны
│   ├── entropy.go        # Анализ энтропии
│   ├── risk_scorer.go    # Оценка рисков
│   ├── reporter.go       # Генерация отчётов
│   ├── ignorelist.go     # Списки игнорирования
│   ├── luhn.go          # Валидация банковских карт
│   └── types.go         # Типы данных
├── testdata/             # Тестовые данные
│   ├── sample.env
│   ├── leaked_card.txt
│   ├── private.pem
│   └── secrets.json
├── go.mod
├── go.sum
├── Makefile
└── README.md
```

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
make test
```

### Запуск с подробным выводом

```bash
go test -v ./...
```

### Тесты с покрытием

```bash
go test -cover ./...
```

### Бенчмарки

```bash
go test -bench=. ./searcher
```

### Тестовые данные

Директория `testdata/` содержит примеры файлов:

| Файл | Содержимое |
|------|------------|
| `sample.env` | Переменные окружения с паролями |
| `leaked_card.txt` | Номер банковской карты |
| `private.pem` | Приватный RSA-ключ |
| `secrets.json` | JSON с API-ключами |
| `config.yaml` | YAML с токенами |

---

## ⚙️ Конфигурация

### Обнаруживаемые паттерны

| Паттерн | Описание | Уровень |
|---------|----------|---------|
| Пароли | password=, passwd=, pwd= | Высокий |
| API-ключи | api_key=, apikey= | Высокий |
| AWS ключи | AKIA..., aws_secret | Критический |
| GitHub токены | ghp_, gho_, ghu_ | Критический |
| JWT токены | eyJ... | Высокий |
| Приватные ключи | BEGIN RSA PRIVATE KEY | Критический |
| Банковские карты | 16-цифровые номера (Luhn) | Критический |
| SSH ключи | BEGIN OPENSSH PRIVATE KEY | Критический |

### Уровни серьёзности

| Уровень | Цвет | Описание |
|---------|------|----------|
| 🔴 Критический | Красный | Требуется немедленное действие |
| 🟠 Высокий | Оранжевый | Следует исправить в ближайшее время |
| 🟡 Средний | Жёлтый | Рекомендуется проверить |
| 🟢 Низкий | Зелёный | Незначительная проблема |

---

## 🔧 Makefile команды

```bash
make build          # Сборка CLI и GUI
make build-cli      # Сборка только CLI
make build-gui      # Сборка только GUI
make build-app      # Сборка macOS .app bundle
make run            # Запуск GUI приложения
make test           # Запуск тестов
make clean          # Очистка артефактов сборки
make install        # Установка в ~/bin
```

---

## 📊 Форматы экспорта

### JSON
```json
{
  "scan_date": "2025-11-30T12:00:00Z",
  "directory": "/path/to/scan",
  "findings": [
    {
      "file_path": "/path/to/file.txt",
      "line_number": 10,
      "pattern_type": "password",
      "severity": "high",
      "risk_score": 85.5,
      "matched_text": "pass****word"
    }
  ]
}
```

### CSV
```csv
file_path,line_number,pattern_type,severity,risk_score,description
/path/to/file.txt,10,password,high,85.5,"Password detected"
```

---

## 🐛 Решение проблем

### GUI не запускается на macOS

Fyne приложения на macOS требуют упаковки в .app bundle:

```bash
make build-app
make run
```

### Ошибка "operation not permitted"

Убедитесь, что у вас есть права на чтение сканируемой директории:

```bash
ls -la /path/to/scan
```

### Ошибки сборки GUI

Установите необходимые зависимости для вашей ОС (см. раздел "Требования").

---

## 📜 Лицензия

MIT License

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing`)
3. Закоммитьте изменения (`git commit -m 'Добавлена крутая фича'`)
4. Запушьте ветку (`git push origin feature/amazing`)
5. Откройте Pull Request

---

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.
