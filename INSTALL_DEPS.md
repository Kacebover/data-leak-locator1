# Установка дополнительных зависимостей

Данное руководство описывает установку внешних инструментов для расширенных функций сканирования.

## 🔔 Автоматическая проверка зависимостей

Приложение **автоматически проверяет** наличие зависимостей:

- **CLI**: При запуске сканирования показывает предупреждения если зависимости отсутствуют
- **GUI**: При старте скана показывает диалог с информацией об отсутствующих зависимостях и предлагает продолжить или установить

Если зависимость не установлена:
- ⚠️ Показывается предупреждение с инструкцией по установке
- 📝 Предлагается команда для установки под вашу ОС
- ✅ Сканирование продолжается с ограниченным функционалом

## 🔍 Что работает "из коробки"

Без дополнительных зависимостей приложение может:
- ✅ Сканировать текстовые файлы (.txt, .json, .yaml, .env, .go, .py и др.)
- ✅ Извлекать текст из DOCX/XLSX (они основаны на ZIP+XML)
- ✅ Сканировать содержимое ZIP/TAR архивов
- ✅ Базовое извлечение текста из некоторых PDF

## 📷 OCR для изображений (Tesseract)

Для распознавания текста на изображениях (PNG, JPG) требуется **Tesseract OCR**.

### macOS

```bash
# Через Homebrew
brew install tesseract

# Дополнительные языки (русский, английский уже включены)
brew install tesseract-lang
```

### Ubuntu/Debian

```bash
sudo apt update
sudo apt install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
```

### Fedora/CentOS/RHEL

```bash
sudo dnf install tesseract tesseract-langpack-rus tesseract-langpack-eng
```

### Arch Linux

```bash
sudo pacman -S tesseract tesseract-data-rus tesseract-data-eng
```

### Windows

**Вариант 1: Через Chocolatey (рекомендуется)**
```powershell
# Установка Chocolatey (если не установлен)
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установка Tesseract
choco install tesseract
```

**Вариант 2: Ручная установка**
1. Скачайте установщик с [GitHub UB-Mannheim](https://github.com/UB-Mannheim/tesseract/wiki)
2. Установите в `C:\Program Files\Tesseract-OCR`
3. Добавьте путь в переменную `PATH`:
   ```powershell
   # PowerShell (от администратора)
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\Program Files\Tesseract-OCR", "Machine")
   ```
   Или вручную: "Параметры системы" → "Переменные среды" → Добавить в PATH

### Проверка установки

```bash
# Linux/macOS
tesseract --version

# Windows (PowerShell)
tesseract --version
```

Должен показать версию и доступные языки:
```
tesseract 5.x.x
 Available languages: eng, rus
```

## 📄 Расширенное извлечение PDF

Для более качественного извлечения текста из PDF рекомендуется **Poppler** (включает `pdftotext` и `pdftoppm`).

**Важно**: Для сканированных PDF (без текстового слоя) требуется:
- `pdftoppm` — конвертация PDF в изображения
- `tesseract` — OCR изображений

### macOS

```bash
brew install poppler
```

### Ubuntu/Debian

```bash
sudo apt install poppler-utils
```

### Fedora/CentOS

```bash
sudo dnf install poppler-utils
```

### Windows

**Вариант 1: Через Chocolatey**
```powershell
choco install poppler
```

**Вариант 2: Ручная установка**
1. Скачайте [Poppler для Windows](https://github.com/oschwartz10612/poppler-windows/releases)
2. Распакуйте в `C:\Program Files\poppler`
3. Добавьте в PATH:
   ```powershell
   # PowerShell (от администратора)
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\Program Files\poppler\bin", "Machine")
   ```

### Проверка

```bash
# Linux/macOS
pdftotext -v
pdftoppm -v

# Windows (CMD/PowerShell)
pdftotext -v
pdftoppm -v
```

## 🤖 AI-анализ (Ollama)

Для локального AI-анализа требуется **Ollama**.

### macOS

```bash
# Через Homebrew
brew install ollama

# Запуск сервиса
brew services start ollama

# Загрузка модели
ollama pull llama3.2
```

### Linux

```bash
curl -fsSL https://ollama.com/install.sh | sh

# Запуск
ollama serve &

# Загрузка модели
ollama pull llama3.2
```

### Windows

1. Скачайте установщик с [ollama.com/download](https://ollama.com/download)
2. Запустите установщик и следуйте инструкциям
3. После установки откройте PowerShell/CMD:
   ```powershell
   # Загрузка модели (выберите одну)
   ollama pull llama3.2       # ~2GB, быстрая
   ollama pull llama3.2:7b    # ~4GB, более точная
   ollama pull qwen2.5:7b     # ~4GB, хорошо работает с русским
   ```

### Проверка

```bash
# Проверка установленных моделей
ollama list

# Проверка работы сервера
curl http://localhost:11434/api/tags
# или в браузере: http://localhost:11434

# Тестовый запрос
ollama run llama3.2 "Привет!"
```

## 📋 Сводная таблица

| Функция | Инструмент | macOS | Linux | Windows |
|---------|------------|-------|-------|---------|
| OCR изображений | Tesseract | `brew install tesseract` | `apt install tesseract-ocr` | Установщик |
| PDF текст | Poppler | `brew install poppler` | `apt install poppler-utils` | Бинарники |
| AI-анализ | Ollama | `brew install ollama` | install.sh | Установщик |

## 🚀 Использование

После установки зависимостей:

### CLI

```bash
# Сканирование с OCR
./data-leak-locator scan -dir /путь -ocr

# Сканирование документов
./data-leak-locator scan -dir /путь -docs

# Полное сканирование с AI
./data-leak-locator scan -dir /путь -docs -archives -ocr -ai
```

### GUI

1. Запустите приложение
2. Отметьте нужные опции:
   - ✅ Извлекать текст из PDF/DOCX/XLSX
   - ✅ Сканировать внутри архивов  
   - ✅ OCR для изображений
   - ✅ AI-анализ после скана

## ⚠️ Важно

- **OCR требует много ресурсов** — сканирование может занять время
- **Tesseract работает лучше** с качественными изображениями (≥300 DPI)
- **Ollama требует** ~4-8 GB RAM для работы моделей
- **Для изображений документов** рекомендуется включать OCR

## 🔧 Troubleshooting

### Tesseract не найден

```bash
# macOS: проверьте путь
which tesseract
ls /opt/homebrew/bin/tesseract

# Linux
which tesseract
ls /usr/bin/tesseract
```

### OCR возвращает пустой текст

- Убедитесь, что изображение не повреждено
- Проверьте качество изображения (рекомендуется ≥150 DPI)
- Попробуйте повернуть изображение (Tesseract чувствителен к ориентации)

### Ollama не отвечает

```bash
# Проверьте статус
curl http://localhost:11434/api/tags

# Перезапустите
ollama serve
```

